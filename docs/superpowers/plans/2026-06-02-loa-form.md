# Form LoA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Membangun form create/edit LoA draft berupa wizard 4 langkah dengan drill-down pilih menu, kalkulasi harga otomatis, dan simpan atomik.

**Architecture:** Client-state wizard (`useReducer` + context) menahan seluruh data; logika harga & validasi ditarik ke fungsi murni di `lib/loa/` (unit-tested); satu server action menulis `loa` + `loa_items` + `loa_item_selections` secara atomik. Pendekatan A dari spec.

**Tech Stack:** Next.js 16 (App Router, Server Actions), Supabase, shadcn/ui (sheet, select, radio-group, checkbox), Vitest (baru), zod, TypeScript.

**Spec:** `docs/superpowers/specs/2026-06-02-loa-form-design.md`

---

## Catatan Konvensi (baca sebelum mulai)

- Tabel DB bernama `orders` (bukan `bookings`); kolom `loa.booking_id` me-refer `orders(id)`.
- Server client: `import { createClient } from '@/lib/supabase/server'` → `await createClient()`.
- Auth: `import { getAppUser } from '@/lib/auth/permissions'` → `await getAppUser()`; cek izin via `user.permissions['loa.create']`.
- Return server action bertipe `ActionResult<T>` (discriminated union) dari `@/types/domain`:
  `{ success: true; data: T } | { success: false; error: string }`.
- SQL dijalankan manual di Supabase SQL Editor (lihat `docs/DEVELOPMENT_GUIDE.md`).
- Path tes: letakkan `*.test.ts` bersebelahan dengan file sumber di `lib/loa/`.

---

## FASE 0 — Infrastruktur & Schema

### Task 1: Setup Vitest

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`

- [ ] **Step 1: Install Vitest**

Run:
```bash
npm install -D vitest@^2
```

- [ ] **Step 2: Buat config**

Create `vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config'
import path from 'node:path'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['lib/**/*.test.ts'],
  },
  resolve: {
    alias: { '@': path.resolve(__dirname, '.') },
  },
})
```

- [ ] **Step 3: Tambah script test**

Modify `package.json` scripts menjadi:
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint",
  "test": "vitest run",
  "test:watch": "vitest"
}
```

- [ ] **Step 4: Verifikasi runner jalan**

Run: `npx vitest run`
Expected: keluar "No test files found" (belum ada tes) — exit code 0, runner terpasang.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json vitest.config.ts
git commit -m "chore: setup Vitest untuk unit test lib/"
```

---

### Task 2: Migration 002 — kolom diskon + update types

**Files:**
- Create: `db/migrations/002_loa_discount.sql`
- Modify: `types/database.ts` (tabel `loa`: Row/Insert/Update)

- [ ] **Step 1: Tulis migration**

Create `db/migrations/002_loa_discount.sql`:
```sql
-- Tambah mode diskon (percent/flat) ke LoA.
-- Kolom `discount` lama tetap menyimpan hasil amount (dipakai IB/laporan).
ALTER TABLE loa
  ADD COLUMN discount_type  VARCHAR(10) NOT NULL DEFAULT 'flat'
    CHECK (discount_type IN ('percent','flat')),
  ADD COLUMN discount_value NUMERIC(15,2) NOT NULL DEFAULT 0;

COMMENT ON COLUMN loa.discount_type IS 'percent = discount_value adalah %, flat = discount_value adalah Rupiah.';
COMMENT ON COLUMN loa.discount_value IS 'Nilai mentah diskon sesuai discount_type. Hasil amount disimpan di kolom discount.';
```

- [ ] **Step 2: Jalankan di Supabase**

Buka Supabase Dashboard → SQL Editor → New Query → paste isi `002_loa_discount.sql` → Run.
Expected: "Success. No rows returned".

- [ ] **Step 3: Update types/database.ts**

Di `types/database.ts`, pada `loa` → `Row`, tambahkan:
```ts
discount_type: string
discount_value: number
```
Pada `loa` → `Insert` dan `Update`, tambahkan:
```ts
discount_type?: string
discount_value?: number
```
(Cari blok `loa: { Row: {...} Insert: {...} Update: {...} }` dan sisipkan field di ketiganya.)

- [ ] **Step 4: Verifikasi typecheck**

Run: `npx tsc --noEmit`
Expected: tidak ada error baru terkait `loa` (abaikan error stale `.next/` lama jika ada).

- [ ] **Step 5: Commit**

```bash
git add db/migrations/002_loa_discount.sql types/database.ts
git commit -m "feat(db): migration 002 — kolom diskon LoA (discount_type + discount_value)"
```

---

### Task 3: Tambah komponen radio-group & checkbox

**Files:**
- Create: `components/ui/radio-group.tsx`, `components/ui/checkbox.tsx`

- [ ] **Step 1: Tambah via shadcn**

Run:
```bash
npx shadcn@latest add radio-group checkbox
```
Jika prompt overwrite/config muncul, terima default (project sudah pakai shadcn).

- [ ] **Step 2: Verifikasi file ada**

Run: `ls components/ui/radio-group.tsx components/ui/checkbox.tsx`
Expected: kedua file tampil.

- [ ] **Step 3: Commit**

```bash
git add components/ui/radio-group.tsx components/ui/checkbox.tsx
git commit -m "feat(ui): tambah radio-group & checkbox untuk drawer menu LoA"
```

---

## FASE 1 — Logika Murni (`lib/loa/`, full TDD)

### Task 4: Kalkulasi harga — `lib/loa/calculations.ts`

**Files:**
- Create: `lib/loa/calculations.ts`
- Test: `lib/loa/calculations.test.ts`

- [ ] **Step 1: Tulis tes yang gagal**

Create `lib/loa/calculations.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { calculateLoa, PB1_PCT } from './calculations'

const items = [{ pricePerPax: 150000, pax: 50 }] // sub_total_1 = 7.500.000

describe('calculateLoa', () => {
  it('hitung breakdown default (SC 5%, handling 15%, tanpa diskon)', () => {
    const r = calculateLoa(items, { scPct: 5, handlingPct: 15, discountType: 'flat', discountValue: 0 })
    expect(r.subTotal1).toBe(7500000)
    expect(r.serviceChargeAmt).toBe(375000)
    expect(r.subTotal2).toBe(7875000)
    expect(r.pb1Amt).toBe(787500)
    expect(r.handlingFeeAmt).toBe(1181250)
    expect(r.discountAmt).toBe(0)
    expect(r.grandTotal).toBe(9843750)
    expect(r.netRevenue).toBe(7875000)
  })

  it('PB1 konstan 10%', () => {
    expect(PB1_PCT).toBe(10)
  })

  it('diskon flat dipotong dari grand total, net revenue tetap penuh', () => {
    const r = calculateLoa(items, { scPct: 5, handlingPct: 15, discountType: 'flat', discountValue: 843750 })
    expect(r.discountAmt).toBe(843750)
    expect(r.grandTotal).toBe(9000000)
    expect(r.netRevenue).toBe(7875000) // tidak terpengaruh diskon
  })

  it('diskon percent = % dari grand total pre-discount', () => {
    const r = calculateLoa(items, { scPct: 5, handlingPct: 15, discountType: 'percent', discountValue: 10 })
    expect(r.discountAmt).toBe(984375) // 10% dari 9.843.750
    expect(r.grandTotal).toBe(8859375)
    expect(r.netRevenue).toBe(7875000)
  })

  it('diskon di-clamp tidak melebihi grand total pre-discount', () => {
    const r = calculateLoa(items, { scPct: 5, handlingPct: 15, discountType: 'flat', discountValue: 99999999 })
    expect(r.discountAmt).toBe(9843750)
    expect(r.grandTotal).toBe(0)
  })

  it('beberapa item dijumlahkan', () => {
    const r = calculateLoa(
      [{ pricePerPax: 100000, pax: 10 }, { pricePerPax: 50000, pax: 20 }],
      { scPct: 5, handlingPct: 15, discountType: 'flat', discountValue: 0 }
    )
    expect(r.subTotal1).toBe(2000000)
  })
})
```

- [ ] **Step 2: Jalankan tes — pastikan gagal**

Run: `npx vitest run lib/loa/calculations.test.ts`
Expected: FAIL — "Failed to resolve import './calculations'".

- [ ] **Step 3: Implementasi**

Create `lib/loa/calculations.ts`:
```ts
export const PB1_PCT = 10

export interface LoaCalcItem {
  pricePerPax: number
  pax: number
}

export interface LoaPricingInput {
  scPct: number
  handlingPct: number
  discountType: 'percent' | 'flat'
  discountValue: number
}

export interface LoaCalcResult {
  subTotal1: number
  serviceChargeAmt: number
  subTotal2: number
  pb1Amt: number
  handlingFeeAmt: number
  discountAmt: number
  grandTotal: number
  netRevenue: number
}

function round2(n: number): number {
  return Math.round(n * 100) / 100
}

export function calculateLoa(
  items: LoaCalcItem[],
  pricing: LoaPricingInput
): LoaCalcResult {
  const subTotal1 = round2(items.reduce((sum, i) => sum + i.pricePerPax * i.pax, 0))
  const serviceChargeAmt = round2((subTotal1 * pricing.scPct) / 100)
  const subTotal2 = round2(subTotal1 + serviceChargeAmt)
  const pb1Amt = round2((subTotal2 * PB1_PCT) / 100)
  const handlingFeeAmt = round2((subTotal2 * pricing.handlingPct) / 100)
  const grandPreDiscount = round2(subTotal2 + pb1Amt + handlingFeeAmt)

  const rawDiscount =
    pricing.discountType === 'percent'
      ? round2((grandPreDiscount * pricing.discountValue) / 100)
      : pricing.discountValue
  const discountAmt = round2(Math.min(Math.max(rawDiscount, 0), grandPreDiscount))

  return {
    subTotal1,
    serviceChargeAmt,
    subTotal2,
    pb1Amt,
    handlingFeeAmt,
    discountAmt,
    grandTotal: round2(grandPreDiscount - discountAmt),
    netRevenue: subTotal2,
  }
}
```

- [ ] **Step 4: Jalankan tes — pastikan lulus**

Run: `npx vitest run lib/loa/calculations.test.ts`
Expected: PASS (6 tes).

- [ ] **Step 5: Commit**

```bash
git add lib/loa/calculations.ts lib/loa/calculations.test.ts
git commit -m "feat(loa): fungsi kalkulasi harga LoA (murni, tested)"
```

---

### Task 5: Validasi pilihan menu — `lib/loa/selection-rules.ts`

**Files:**
- Create: `lib/loa/selection-rules.ts`
- Test: `lib/loa/selection-rules.test.ts`

- [ ] **Step 1: Tulis tes yang gagal**

Create `lib/loa/selection-rules.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { validateCategorySelections } from './selection-rules'

describe('validateCategorySelections', () => {
  it('rule one harus tepat 1 — kosong = error', () => {
    const errors = validateCategorySelections([
      { categoryId: 'c1', categoryName: 'Minuman', rule: 'one', selectedItemIds: [] },
    ])
    expect(errors).toHaveLength(1)
    expect(errors[0].categoryId).toBe('c1')
  })

  it('rule one dengan >1 terpilih = error', () => {
    const errors = validateCategorySelections([
      { categoryId: 'c1', categoryName: 'Minuman', rule: 'one', selectedItemIds: ['a', 'b'] },
    ])
    expect(errors).toHaveLength(1)
  })

  it('rule one dengan tepat 1 = valid', () => {
    const errors = validateCategorySelections([
      { categoryId: 'c1', categoryName: 'Minuman', rule: 'one', selectedItemIds: ['a'] },
    ])
    expect(errors).toHaveLength(0)
  })

  it('rule multiple butuh minimal 1', () => {
    expect(
      validateCategorySelections([
        { categoryId: 'c2', categoryName: 'Savoury', rule: 'multiple', selectedItemIds: [] },
      ])
    ).toHaveLength(1)
    expect(
      validateCategorySelections([
        { categoryId: 'c2', categoryName: 'Savoury', rule: 'multiple', selectedItemIds: ['x'] },
      ])
    ).toHaveLength(0)
  })

  it('gabungan beberapa kategori melaporkan semua yang gagal', () => {
    const errors = validateCategorySelections([
      { categoryId: 'c1', categoryName: 'Minuman', rule: 'one', selectedItemIds: [] },
      { categoryId: 'c2', categoryName: 'Savoury', rule: 'multiple', selectedItemIds: ['x'] },
      { categoryId: 'c3', categoryName: 'Dessert', rule: 'one', selectedItemIds: ['y', 'z'] },
    ])
    expect(errors.map((e) => e.categoryId).sort()).toEqual(['c1', 'c3'])
  })
})
```

- [ ] **Step 2: Jalankan tes — pastikan gagal**

Run: `npx vitest run lib/loa/selection-rules.test.ts`
Expected: FAIL — import tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `lib/loa/selection-rules.ts`:
```ts
export type SelectionRule = 'one' | 'multiple'

export interface CategorySelectionState {
  categoryId: string
  categoryName: string
  rule: SelectionRule
  selectedItemIds: string[]
}

export interface SelectionValidationError {
  categoryId: string
  message: string
}

export function validateCategorySelections(
  categories: CategorySelectionState[]
): SelectionValidationError[] {
  const errors: SelectionValidationError[] = []
  for (const cat of categories) {
    const n = cat.selectedItemIds.length
    if (cat.rule === 'one' && n !== 1) {
      errors.push({ categoryId: cat.categoryId, message: `${cat.categoryName}: pilih tepat 1` })
    }
    if (cat.rule === 'multiple' && n < 1) {
      errors.push({ categoryId: cat.categoryId, message: `${cat.categoryName}: pilih minimal 1` })
    }
  }
  return errors
}
```

- [ ] **Step 4: Jalankan tes — pastikan lulus**

Run: `npx vitest run lib/loa/selection-rules.test.ts`
Expected: PASS (5 tes).

- [ ] **Step 5: Commit**

```bash
git add lib/loa/selection-rules.ts lib/loa/selection-rules.test.ts
git commit -m "feat(loa): validasi aturan pilih menu one/multiple (tested)"
```

---

### Task 6: Generate teks menu_detail — `lib/loa/menu-detail.ts`

**Files:**
- Create: `lib/loa/menu-detail.ts`
- Test: `lib/loa/menu-detail.test.ts`

- [ ] **Step 1: Tulis tes yang gagal**

Create `lib/loa/menu-detail.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { generateMenuDetail } from './menu-detail'

describe('generateMenuDetail', () => {
  it('kelompokkan per komponen+occasion, gabung item dengan koma', () => {
    const text = generateMenuDetail([
      { componentName: 'Coffee Break', occasionNo: 1, categoryName: 'Savoury', itemName: 'Risoles' },
      { componentName: 'Coffee Break', occasionNo: 1, categoryName: 'Minuman', itemName: 'Teh' },
      { componentName: 'Coffee Break', occasionNo: 2, categoryName: 'Savoury', itemName: 'Pastel' },
      { componentName: 'Lunch', occasionNo: 1, categoryName: 'Main', itemName: 'Ayam' },
    ])
    expect(text).toBe('Coffee Break 1: Risoles, Teh · Coffee Break 2: Pastel · Lunch: Ayam')
  })

  it('occasion tunggal tidak diberi nomor', () => {
    const text = generateMenuDetail([
      { componentName: 'Lunch', occasionNo: 1, categoryName: 'Main', itemName: 'Ayam' },
    ])
    expect(text).toBe('Lunch: Ayam')
  })

  it('kosong = string kosong', () => {
    expect(generateMenuDetail([])).toBe('')
  })
})
```

- [ ] **Step 2: Jalankan tes — pastikan gagal**

Run: `npx vitest run lib/loa/menu-detail.test.ts`
Expected: FAIL — import tidak ditemukan.

- [ ] **Step 3: Implementasi**

Create `lib/loa/menu-detail.ts`:
```ts
export interface SelectionForDetail {
  componentName: string
  occasionNo: number
  categoryName: string
  itemName: string
}

export function generateMenuDetail(selections: SelectionForDetail[]): string {
  // Kelompokkan per "componentName||occasionNo", jaga urutan kemunculan.
  const groups = new Map<string, { component: string; occasion: number; items: string[] }>()
  for (const s of selections) {
    const key = `${s.componentName}||${s.occasionNo}`
    if (!groups.has(key)) {
      groups.set(key, { component: s.componentName, occasion: s.occasionNo, items: [] })
    }
    groups.get(key)!.items.push(s.itemName)
  }

  // Hitung jumlah occasion per komponen → tentukan apakah perlu nomor sesi.
  const occasionCount = new Map<string, number>()
  for (const g of groups.values()) {
    occasionCount.set(g.component, (occasionCount.get(g.component) ?? 0) + 1)
  }

  const parts: string[] = []
  for (const g of groups.values()) {
    const label = (occasionCount.get(g.component) ?? 1) > 1 ? `${g.component} ${g.occasion}` : g.component
    parts.push(`${label}: ${g.items.join(', ')}`)
  }
  return parts.join(' · ')
}
```

- [ ] **Step 4: Jalankan tes — pastikan lulus**

Run: `npx vitest run lib/loa/menu-detail.test.ts`
Expected: PASS (3 tes).

- [ ] **Step 5: Commit**

```bash
git add lib/loa/menu-detail.ts lib/loa/menu-detail.test.ts
git commit -m "feat(loa): generate teks menu_detail dari selections (tested)"
```

---

### Task 7: Nomor dokumen LoA — `lib/loa/doc-no.ts`

**Files:**
- Create: `lib/loa/doc-no.ts`

- [ ] **Step 1: Implementasi (pola dari `lib/utils/order-no.ts`)**

Create `lib/loa/doc-no.ts`:
```ts
import { createClient } from '@/lib/supabase/server'

/** Generate nomor dokumen LoA format LOA-YYYYMM-XXX (urut per bulan berjalan). */
export async function generateLoaDocNo(refDate: Date = new Date()): Promise<string> {
  const year = refDate.getFullYear()
  const month = String(refDate.getMonth() + 1).padStart(2, '0')
  const prefix = `LOA-${year}${month}-`

  const supabase = await createClient()
  const { data } = await supabase
    .from('loa')
    .select('doc_no')
    .like('doc_no', `${prefix}%`)
    .order('doc_no', { ascending: false })
    .limit(1)

  let seq = 1
  if (data && data.length > 0) {
    const parts = data[0].doc_no.split('-')
    const lastSeq = parseInt(parts[parts.length - 1], 10)
    if (!isNaN(lastSeq)) seq = lastSeq + 1
  }
  return `${prefix}${String(seq).padStart(3, '0')}`
}
```

- [ ] **Step 2: Verifikasi typecheck**

Run: `npx tsc --noEmit`
Expected: tidak ada error baru di `lib/loa/doc-no.ts`.

- [ ] **Step 3: Commit**

```bash
git add lib/loa/doc-no.ts
git commit -m "feat(loa): generator nomor dokumen LOA-YYYYMM-XXX"
```

---

## FASE 2 — Tipe Draft, Loader Katalog, Server Action

### Task 8: Tipe state wizard — `features/loa/types.ts`

**Files:**
- Create: `features/loa/types.ts`

- [ ] **Step 1: Implementasi**

Create `features/loa/types.ts`:
```ts
import type { SelectionRule } from '@/lib/loa/selection-rules'

// ---- Katalog (di-load server-side, read-only) ----
export interface CatalogItem { id: string; nama: string }
export interface CatalogCategory {
  id: string
  componentType: string
  nama: string
  rule: SelectionRule
  items: CatalogItem[]
}
export interface CatalogComponent {
  componentType: string
  nama: string
  qty: number
  sortOrder: number
}
export interface CatalogPackage {
  id: string
  namaPaket: string
  kategori: string
  hargaPerPax: number | null
  hargaMinimum: number | null
  hasSelection: boolean
  components: CatalogComponent[]
}
export interface MenuCatalog {
  packages: CatalogPackage[]
  /** kategori + item, dikelompokkan per componentType */
  categoriesByComponentType: Record<string, CatalogCategory[]>
}

// ---- State draft di client ----
export interface DraftSelection {
  componentName: string
  occasionNo: number
  categoryId: string
  categoryName: string
  itemId: string
  itemName: string
}
export interface LoaItemDraft {
  key: string            // id sementara di client (crypto.randomUUID)
  packageId: string | null
  packageName: string
  pricePerPax: number
  pax: number
  selections: DraftSelection[]
}
export interface LoaDetailDraft {
  setupLocation: string
}
export interface LoaPricingDraft {
  scPct: number
  handlingPct: number
  discountType: 'percent' | 'flat'
  discountValue: number
}
export interface LoaWizardState {
  detail: LoaDetailDraft
  items: LoaItemDraft[]
  pricing: LoaPricingDraft
}

export const DEFAULT_PRICING: LoaPricingDraft = {
  scPct: 5,
  handlingPct: 15,
  discountType: 'flat',
  discountValue: 0,
}
```

- [ ] **Step 2: Verifikasi typecheck**

Run: `npx tsc --noEmit`
Expected: tidak ada error baru.

- [ ] **Step 3: Commit**

```bash
git add features/loa/types.ts
git commit -m "feat(loa): tipe state wizard & katalog menu"
```

---

### Task 9: Loader katalog menu — `lib/loa/catalog.ts`

**Files:**
- Create: `lib/loa/catalog.ts`

- [ ] **Step 1: Implementasi (server-side fetch + shaping)**

Create `lib/loa/catalog.ts`:
```ts
import { createClient } from '@/lib/supabase/server'
import type { MenuCatalog, CatalogCategory } from '@/features/loa/types'
import type { SelectionRule } from '@/lib/loa/selection-rules'

/** Ambil seluruh katalog menu untuk form LoA dan bentuk jadi MenuCatalog. */
export async function loadMenuCatalog(): Promise<MenuCatalog> {
  const supabase = await createClient()

  const [pkgRes, compRes, catRes, itemRes] = await Promise.all([
    supabase.from('menu_packages').select('id, nama_paket, kategori, harga_per_pax, harga_minimum, has_selection').eq('is_active', true).order('nama_paket'),
    supabase.from('menu_package_components').select('package_id, component_type, nama, qty, sort_order').order('sort_order'),
    supabase.from('menu_catalog_categories').select('id, component_type, nama, selection_rule, sort_order').order('sort_order'),
    supabase.from('menu_catalog_items').select('id, category_id, nama').eq('is_active', true).order('sort_order'),
  ])

  const items = itemRes.data ?? []
  const itemsByCat = new Map<string, { id: string; nama: string }[]>()
  for (const it of items) {
    const arr = itemsByCat.get(it.category_id) ?? []
    arr.push({ id: it.id, nama: it.nama })
    itemsByCat.set(it.category_id, arr)
  }

  const categoriesByComponentType: Record<string, CatalogCategory[]> = {}
  for (const c of catRes.data ?? []) {
    const cat: CatalogCategory = {
      id: c.id,
      componentType: c.component_type,
      nama: c.nama,
      rule: (c.selection_rule === 'one' ? 'one' : 'multiple') as SelectionRule,
      items: itemsByCat.get(c.id) ?? [],
    }
    ;(categoriesByComponentType[c.component_type] ??= []).push(cat)
  }

  const compsByPkg = new Map<string, { componentType: string; nama: string; qty: number; sortOrder: number }[]>()
  for (const cp of compRes.data ?? []) {
    const arr = compsByPkg.get(cp.package_id) ?? []
    arr.push({ componentType: cp.component_type, nama: cp.nama, qty: cp.qty, sortOrder: cp.sort_order })
    compsByPkg.set(cp.package_id, arr)
  }

  const packages = (pkgRes.data ?? []).map((p) => ({
    id: p.id,
    namaPaket: p.nama_paket,
    kategori: p.kategori,
    hargaPerPax: p.harga_per_pax,
    hargaMinimum: p.harga_minimum,
    hasSelection: p.has_selection,
    components: compsByPkg.get(p.id) ?? [],
  }))

  return { packages, categoriesByComponentType }
}
```

- [ ] **Step 2: Verifikasi typecheck**

Run: `npx tsc --noEmit`
Expected: tidak ada error baru. (Jika nama kolom berbeda dari schema, sesuaikan select.)

- [ ] **Step 3: Commit**

```bash
git add lib/loa/catalog.ts
git commit -m "feat(loa): loader katalog menu (packages/components/categories/items)"
```

---

### Task 10: Server action — `features/loa/actions.ts`

**Files:**
- Create: `features/loa/actions.ts`

Tujuan: `saveLoaDraft` (simpan atomik: re-validasi + re-kalkulasi server, upsert `loa`, delete-and-reinsert `loa_items` + `loa_item_selections`) dan `getLoaForEdit` (muat LoA existing jadi `LoaWizardState`).

- [ ] **Step 1: Implementasi**

Create `features/loa/actions.ts`:
```ts
'use server'

import { revalidatePath } from 'next/cache'
import { createClient } from '@/lib/supabase/server'
import { getAppUser } from '@/lib/auth/permissions'
import { generateLoaDocNo } from '@/lib/loa/doc-no'
import { calculateLoa } from '@/lib/loa/calculations'
import { generateMenuDetail } from '@/lib/loa/menu-detail'
import type { ActionResult } from '@/types/domain'
import type { LoaWizardState } from './types'

export async function saveLoaDraft(
  orderId: string,
  state: LoaWizardState
): Promise<ActionResult<{ id: string; doc_no: string }>> {
  const user = await getAppUser()
  if (!user) return { success: false, error: 'Tidak terautentikasi' }
  if (!user.permissions['loa.create']) return { success: false, error: 'Tidak ada izin' }
  if (state.items.length === 0) return { success: false, error: 'Minimal 1 item' }

  const supabase = await createClient()

  // Pastikan order milik sales ini (RLS lapis kedua di server)
  const { data: order } = await supabase
    .from('orders').select('id, sales_id').eq('id', orderId).single()
  if (!order) return { success: false, error: 'Order tidak ditemukan' }
  if (!user.permissions['orders.view_all'] && order.sales_id !== user.id) {
    return { success: false, error: 'Bukan order Anda' }
  }

  // Re-kalkulasi di server (jangan percaya angka client)
  const calc = calculateLoa(
    state.items.map((i) => ({ pricePerPax: i.pricePerPax, pax: i.pax })),
    state.pricing
  )

  // Cek LoA existing (1:1 dengan order)
  const { data: existing } = await supabase
    .from('loa').select('id, doc_no').eq('booking_id', orderId).maybeSingle()

  const docNo = existing?.doc_no ?? (await generateLoaDocNo())

  const loaPayload = {
    booking_id: orderId,
    doc_no: docNo,
    status: 'draft' as const,
    setup_location: state.detail.setupLocation || null,
    service_charge_pct: state.pricing.scPct,
    handling_fee_pct: state.pricing.handlingPct,
    discount_type: state.pricing.discountType,
    discount_value: state.pricing.discountValue,
    discount: calc.discountAmt,
    sub_total_1: calc.subTotal1,
    service_charge_amt: calc.serviceChargeAmt,
    sub_total_2: calc.subTotal2,
    pb1_amt: calc.pb1Amt,
    handling_fee_amt: calc.handlingFeeAmt,
    grand_total: calc.grandTotal,
    net_revenue: calc.netRevenue,
    created_by: user.id,
  }

  // Upsert header
  let loaId: string
  if (existing) {
    const { error } = await supabase.from('loa').update(loaPayload).eq('id', existing.id)
    if (error) return { success: false, error: error.message }
    loaId = existing.id
    // Hapus anak lama
    await supabase.from('loa_items').delete().eq('loa_id', loaId)
  } else {
    const { data, error } = await supabase.from('loa').insert(loaPayload).select('id').single()
    if (error || !data) return { success: false, error: error?.message ?? 'Gagal simpan LoA' }
    loaId = data.id
  }

  // Insert ulang items + selections
  for (let idx = 0; idx < state.items.length; idx++) {
    const item = state.items[idx]
    const amount = Math.round(item.pricePerPax * item.pax * 100) / 100
    const { data: insItem, error: itemErr } = await supabase
      .from('loa_items')
      .insert({
        loa_id: loaId,
        package_name: item.packageName,
        menu_detail: generateMenuDetail(
          item.selections.map((s) => ({
            componentName: s.componentName,
            occasionNo: s.occasionNo,
            categoryName: s.categoryName,
            itemName: s.itemName,
          }))
        ),
        price_per_pax: item.pricePerPax,
        pax: item.pax,
        amount,
        sort_order: idx,
      })
      .select('id')
      .single()
    if (itemErr || !insItem) return { success: false, error: itemErr?.message ?? 'Gagal simpan item' }

    if (item.selections.length > 0) {
      const { error: selErr } = await supabase.from('loa_item_selections').insert(
        item.selections.map((s, sIdx) => ({
          loa_item_id: insItem.id,
          component_name: s.componentName,
          occasion_no: s.occasionNo,
          category_name: s.categoryName,
          item_name: s.itemName,
          sort_order: sIdx,
        }))
      )
      if (selErr) return { success: false, error: selErr.message }
    }
  }

  revalidatePath(`/orders/${orderId}/loa`)
  return { success: true, data: { id: loaId, doc_no: docNo } }
}

export async function getLoaForEdit(orderId: string): Promise<LoaWizardState | null> {
  const supabase = await createClient()
  const { data: loa } = await supabase
    .from('loa')
    .select('id, setup_location, service_charge_pct, handling_fee_pct, discount_type, discount_value, loa_items(id, package_name, price_per_pax, pax, sort_order, loa_item_selections(component_name, occasion_no, category_name, item_name, sort_order))')
    .eq('booking_id', orderId)
    .maybeSingle()
  if (!loa) return null

  type RawItem = {
    id: string; package_name: string; price_per_pax: number; pax: number; sort_order: number
    loa_item_selections: { component_name: string; occasion_no: number; category_name: string; item_name: string; sort_order: number }[]
  }
  const items = (loa.loa_items as RawItem[])
    .sort((a, b) => a.sort_order - b.sort_order)
    .map((it) => ({
      key: crypto.randomUUID(),
      packageId: null,
      packageName: it.package_name,
      pricePerPax: it.price_per_pax,
      pax: it.pax,
      selections: it.loa_item_selections
        .sort((a, b) => a.sort_order - b.sort_order)
        .map((s) => ({
          componentName: s.component_name,
          occasionNo: s.occasion_no,
          categoryId: '',
          categoryName: s.category_name,
          itemId: '',
          itemName: s.item_name,
        })),
    }))

  return {
    detail: { setupLocation: loa.setup_location ?? '' },
    items,
    pricing: {
      scPct: Number(loa.service_charge_pct),
      handlingPct: Number(loa.handling_fee_pct),
      discountType: (loa.discount_type === 'percent' ? 'percent' : 'flat'),
      discountValue: Number(loa.discount_value),
    },
  }
}
```

- [ ] **Step 2: Verifikasi typecheck**

Run: `npx tsc --noEmit`
Expected: tidak ada error baru di `features/loa/actions.ts`. (Jika tipe join Supabase rewel, beri anotasi eksplisit seperti `RawItem`.)

- [ ] **Step 3: Commit**

```bash
git add features/loa/actions.ts
git commit -m "feat(loa): server action saveLoaDraft + getLoaForEdit (atomik, re-validasi server)"
```

---

## FASE 3 — UI Wizard

> Komponen UI di bawah diberi **kontrak (props + tanggung jawab + logika kunci)**, bukan baris-per-baris lengkap, karena bergantung pada styling shadcn yang sudah ada. Ikuti pola komponen `features/orders/components/order-form.tsx` dan `features/leads/components/*` untuk gaya. Setiap task diakhiri verifikasi manual di browser + commit.

### Task 11: Context + reducer — `features/loa/components/loa-wizard-context.tsx`

**Files:**
- Create: `features/loa/components/loa-wizard-context.tsx`

**Tanggung jawab:** menyediakan `LoaWizardState` + dispatcher ke seluruh step. Tidak ada I/O.

- [ ] **Step 1: Implementasi**

Buat context dengan `useReducer`. Actions minimal:
```ts
type Action =
  | { type: 'SET_DETAIL'; detail: Partial<LoaDetailDraft> }
  | { type: 'ADD_ITEM'; item: LoaItemDraft }
  | { type: 'UPDATE_ITEM'; key: string; item: LoaItemDraft }
  | { type: 'REMOVE_ITEM'; key: string }
  | { type: 'SET_PRICING'; pricing: Partial<LoaPricingDraft> }
```
Reducer meng-update bagian terkait secara immutable. Export `LoaWizardProvider({ initialState, children })`, hook `useLoaWizard()` (kembalikan `{ state, dispatch }`), dan selector `useLoaCalc()` yang memanggil `calculateLoa(state.items, state.pricing)` (memoized via `useMemo`).

Contoh inti provider:
```tsx
'use client'
import { createContext, useContext, useMemo, useReducer } from 'react'
import { calculateLoa } from '@/lib/loa/calculations'
import { DEFAULT_PRICING, type LoaWizardState, /* ...action types... */ } from '../types'

const Ctx = createContext<{ state: LoaWizardState; dispatch: React.Dispatch<Action> } | null>(null)

export function LoaWizardProvider({ initialState, children }: { initialState: LoaWizardState; children: React.ReactNode }) {
  const [state, dispatch] = useReducer(reducer, initialState)
  return <Ctx.Provider value={{ state, dispatch }}>{children}</Ctx.Provider>
}
export function useLoaWizard() {
  const v = useContext(Ctx)
  if (!v) throw new Error('useLoaWizard must be used within LoaWizardProvider')
  return v
}
export function useLoaCalc() {
  const { state } = useLoaWizard()
  return useMemo(() => calculateLoa(state.items.map(i => ({ pricePerPax: i.pricePerPax, pax: i.pax })), state.pricing), [state.items, state.pricing])
}
```

- [ ] **Step 2: Verifikasi typecheck** — Run: `npx tsc --noEmit` → tidak ada error baru.
- [ ] **Step 3: Commit** — `git commit -m "feat(loa): wizard context + reducer + selector kalkulasi"`

---

### Task 12: Stepper + panel total — `loa-stepper.tsx`, `price-summary-panel.tsx`

**Files:**
- Create: `features/loa/components/loa-stepper.tsx`
- Create: `features/loa/components/price-summary-panel.tsx`

**Tanggung jawab:**
- `loa-stepper.tsx`: tampilkan 4 langkah (Detail/Item & Menu/Harga/Review). Props: `{ current: number; onStepClick?: (n: number) => void }`. Langkah aktif diisi solid, selesai outline, mendatang abu. `onStepClick` hanya aktif di edit mode (loncat antar-langkah).
- `price-summary-panel.tsx`: panggil `useLoaCalc()`, tampilkan breakdown (Sub Total 1, +SC, =Sub Total 2/Net Revenue, +PB1 10%, +Handling, −Diskon, =Grand Total) dengan `formatRupiah` dari `@/lib/utils/format`. Sticky di samping.

- [ ] **Step 1: Implementasi** kedua komponen (gunakan kelas Tailwind seperti di mockup brainstorm: lingkaran stepper, garis penghubung; panel angka rata kanan).
- [ ] **Step 2: Verifikasi typecheck** — `npx tsc --noEmit`.
- [ ] **Step 3: Commit** — `git commit -m "feat(loa): stepper + panel ringkasan harga"`

---

### Task 13: Step Detail & Step Harga — `steps/step-detail.tsx`, `steps/step-pricing.tsx`

**Files:**
- Create: `features/loa/components/steps/step-detail.tsx`
- Create: `features/loa/components/steps/step-pricing.tsx`

**Tanggung jawab:**
- `step-detail.tsx`: props `{ order, primaryContact }`. Tampilkan No. Order (readonly), Klien + PIC (readonly prefill), input No. Dokumen (readonly — diisi server saat simpan; tampilkan "otomatis"), input Lokasi setup → `dispatch SET_DETAIL`.
- `step-pricing.tsx`: input SC% & Handling% (number), PB1 baris read-only "10%", toggle mode diskon (`RadioGroup` percent/flat) + input nilai → `dispatch SET_PRICING`. Validasi inline: SC/handling 0–100.

- [ ] **Step 1: Implementasi** kedua step (form field controlled dari `state`).
- [ ] **Step 2: Verifikasi typecheck** — `npx tsc --noEmit`.
- [ ] **Step 3: Commit** — `git commit -m "feat(loa): step Detail & step Harga"`

---

### Task 14: Drawer pilih menu — `menu-drawer.tsx` + `menu-selection-group.tsx`

**Files:**
- Create: `features/loa/components/menu-selection-group.tsx`
- Create: `features/loa/components/menu-drawer.tsx`

**Tanggung jawab (inti kompleksitas):**
- `menu-drawer.tsx`: pakai `Sheet` (`components/ui/sheet.tsx`). Props: `{ open, onOpenChange, catalog, initialItem?, onSave: (item: LoaItemDraft) => void }`.
  - Pilih paket via `Select`. Saat pilih: prefill `pricePerPax` dari `hargaPerPax ?? hargaMinimum`; input `pax`.
  - Jika `pkg.hasSelection`: untuk tiap `component` dan tiap occasion `1..qty`, render `<MenuSelectionGroup>` dengan kategori dari `catalog.categoriesByComponentType[component.componentType]`.
  - Susun `CategorySelectionState[]` dari pilihan saat ini → panggil `validateCategorySelections`. Tombol "Simpan Item" disabled bila ada error; tampilkan error per kategori.
  - Saat simpan: bentuk `LoaItemDraft` (termasuk `selections: DraftSelection[]`) → `onSave`.
  - Footer: preview `generateMenuDetail(...)`.
- `menu-selection-group.tsx`: props `{ componentName, occasionNo, categories, value, onChange }`. Render tiap kategori: `rule==='one'` → `RadioGroup`; `rule==='multiple'` → daftar `Checkbox`. Emit perubahan ke parent.

- [ ] **Step 1: Implementasi** `menu-selection-group.tsx` (rule-aware rendering).
- [ ] **Step 2: Implementasi** `menu-drawer.tsx` (rakit komponen + validasi ketat + preview).
- [ ] **Step 3: Verifikasi typecheck** — `npx tsc --noEmit`.
- [ ] **Step 4: Commit** — `git commit -m "feat(loa): drawer pilih menu drill-down + validasi ketat"`

---

### Task 15: Step Item — `steps/step-items.tsx`

**Files:**
- Create: `features/loa/components/steps/step-items.tsx`

**Tanggung jawab:** daftar item (card: nama paket, pax × harga = amount, ringkasan menu_detail). Tombol "+ Tambah Item" buka `MenuDrawer` (mode baru). Klik card → buka drawer mode edit (`initialItem`). Tombol hapus → `dispatch REMOVE_ITEM`. Drawer `onSave` → `ADD_ITEM`/`UPDATE_ITEM`.

- [ ] **Step 1: Implementasi**.
- [ ] **Step 2: Verifikasi typecheck** — `npx tsc --noEmit`.
- [ ] **Step 3: Commit** — `git commit -m "feat(loa): step Item + integrasi drawer"`

---

### Task 16: Step Review + orchestrator — `steps/step-review.tsx`, `loa-wizard.tsx`

**Files:**
- Create: `features/loa/components/steps/step-review.tsx`
- Create: `features/loa/components/loa-wizard.tsx`

**Tanggung jawab:**
- `step-review.tsx`: ringkasan read-only (detail, daftar item + menu, breakdown harga dari `useLoaCalc`). Tombol "Simpan Draft" memanggil `onSave`.
- `loa-wizard.tsx`: orchestrator client. Props `{ orderId, order, primaryContact, catalog, initialState }`. Pegang `current` step (useState). Render `LoaWizardProvider` + `LoaStepper` + step aktif + `PriceSummaryPanel`. Tombol Next/Previous dengan validasi per-langkah (L2 minimal 1 item; L3 SC/handling 0–100). Aksi simpan: panggil `saveLoaDraft(orderId, state)`, tampilkan toast (`sonner`), redirect/refresh saat sukses.

- [ ] **Step 1: Implementasi** `step-review.tsx`.
- [ ] **Step 2: Implementasi** `loa-wizard.tsx` (rakit semua + navigasi + submit).
- [ ] **Step 3: Verifikasi typecheck** — `npx tsc --noEmit`.
- [ ] **Step 4: Commit** — `git commit -m "feat(loa): step Review + orchestrator wizard + submit"`

---

### Task 17: Wire ke route + verifikasi end-to-end

**Files:**
- Modify: `app/(dashboard)/orders/[id]/loa/page.tsx` (ganti placeholder)

- [ ] **Step 1: Implementasi page**

Ganti isi `app/(dashboard)/orders/[id]/loa/page.tsx`: load order + lead + kontak primary, `loadMenuCatalog()`, `getLoaForEdit(id)` (untuk `initialState`; kalau null pakai default state), guard `user.permissions['loa.create']`, render `<LoaWizard ... />`. Pola async params seperti placeholder lama (`const { id } = await params`).

State awal default jika belum ada LoA:
```ts
const initialState = (await getLoaForEdit(id)) ?? {
  detail: { setupLocation: '' },
  items: [],
  pricing: DEFAULT_PRICING,
}
```

- [ ] **Step 2: Typecheck + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: bersih (abaikan error stale `.next/` jika ada — jalankan ulang setelah `npm run build` bila perlu).

- [ ] **Step 3: Verifikasi manual di browser**

Run: `npm run dev`, buka order yang ada → `/orders/<id>/loa`. Checklist:
  - Wizard tampil 4 langkah; panel total di samping.
  - Tambah item → drawer drill-down muncul; kategori "pilih 1" = radio, "pilih bebas" = checkbox.
  - Tombol Simpan Item disabled sampai aturan terpenuhi.
  - Total ter-update live saat tambah item & ubah SC/handling/diskon.
  - Simpan Draft → toast sukses; reload halaman → data ter-load lagi (edit mode).
  - Cek di Supabase: row `loa`, `loa_items`, `loa_item_selections` terisi benar.

- [ ] **Step 4: Commit**

```bash
git add "app/(dashboard)/orders/[id]/loa/page.tsx"
git commit -m "feat(loa): wire form LoA ke route /orders/[id]/loa"
```

---

## Verifikasi Akhir

- [ ] `npx vitest run` → semua unit test `lib/loa/` lulus.
- [ ] `npx tsc --noEmit` → tidak ada error di kode sumber.
- [ ] `npm run build` → sukses (regenerasi tipe route, hapus error stale `.next/`).
- [ ] Flow manual (Task 17 Step 3) lolos semua checklist.

---

## Peta Spec → Task (coverage)

| Spec | Task |
|------|------|
| §2 Wizard 4 langkah | 11,12,13,15,16,17 |
| §3 Drawer menu + validasi ketat | 3,5,14 |
| §4 Kalkulasi + diskon dari grand total | 4 |
| §5 State client + simpan atomik | 8,9,10,11,16 |
| §6 Nomor dokumen | 7 |
| §7 Validasi 3 lapis + RLS server | 5,10,13,16 |
| §8 Edit mode (stepper clickable, load existing) | 9,12,16 |
| §9 Migrasi diskon | 2 |
| §10 Testing | 1,4,5,6 |
| §3 menu_detail snapshot | 6,10 |
